services:
  # Redis Stack: 向量数据库 + 管理界面
  redis:
    image: redis/redis-stack:latest
    container_name: unibrain-redis
    restart: unless-stopped
    ports:
      - "6379:6379"   # Redis 端口
      - "8001:8001"   # RedisInsight 界面
    volumes:
      - redis_data:/data

volumes:
  redis_data:
